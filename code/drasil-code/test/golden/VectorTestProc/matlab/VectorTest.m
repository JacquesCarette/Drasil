% VectorTest.m
% Tests native vector operations.
% - Authors: Drasil
% - Note: Generated by Drasil v0.1-alpha

function result = vectorOps(a, b)
    made = [1.0, 2.0, 3.0];
    scaled = 2.0 * a;
    summed = a + b;
    elem = a(1);
    dotted = dot(a, b);
    dim = length(a);
    mag = norm(a);
    unit = a / norm(a);
    result = dotted;
end
