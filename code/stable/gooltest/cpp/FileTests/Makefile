ifeq "$(OS)" "Windows_NT"
    TARGET_EXTENSION=.exe
else
    UNAME_S := $(shell uname -s)
    ifeq ($(UNAME_S), Linux)
        TARGET_EXTENSION=
    endif
    ifeq ($(UNAME_S), Darwin)
        TARGET_EXTENSION=
    endif
endif

# Generated by Drasil v0.1-alpha

# Project Name: FileTests

build: FileTests$(TARGET_EXTENSION)

FileTests$(TARGET_EXTENSION): FileTests.cpp
	"$(CXX)" FileTests.cpp --std=c++11 -o FileTests$(TARGET_EXTENSION)

run: build
	./FileTests$(TARGET_EXTENSION) $(RUNARGS)

.PHONY: build run
