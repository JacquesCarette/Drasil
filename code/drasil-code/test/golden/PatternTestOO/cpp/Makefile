ifeq "$(OS)" "Windows_NT"
    TARGET_EXTENSION=.exe
else
    UNAME_S := $(shell uname -s)
    ifeq ($(UNAME_S), Linux)
        TARGET_EXTENSION=
    endif
    ifeq ($(UNAME_S), Darwin)
        TARGET_EXTENSION=
    endif
endif

# Generated by Drasil v0.1-alpha

# Project Name: PatternTest

build: PatternTest$(TARGET_EXTENSION)

PatternTest$(TARGET_EXTENSION): Observer.hpp PatternTest.cpp Observer.cpp
	"$(CXX)" PatternTest.cpp Observer.cpp --std=c++11 -o PatternTest$(TARGET_EXTENSION)

run: build
	./PatternTest$(TARGET_EXTENSION) $(RUNARGS)

.PHONY: build run
