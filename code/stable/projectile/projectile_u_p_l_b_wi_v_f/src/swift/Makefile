ifeq "$(OS)" "Windows_NT"
    TARGET_EXTENSION=.exe
else
    UNAME_S := $(shell uname -s)
    ifeq ($(UNAME_S), Linux)
        TARGET_EXTENSION=
    endif
    ifeq ($(UNAME_S), Darwin)
        TARGET_EXTENSION=
    endif
endif

# Generated by Drasil v0.1-alpha

# Project Name: Projectile

# Project Purpose: Predict whether a launched projectile hits its target.

build: Projectile$(TARGET_EXTENSION)

Projectile$(TARGET_EXTENSION): main.swift
	swiftc main.swift -o Projectile$(TARGET_EXTENSION)

run: build
	./Projectile$(TARGET_EXTENSION) $(RUNARGS)

doc: main.swift

.PHONY: build run doc
