ifeq "$(OS)" "Windows_NT"
    TARGET_EXTENSION=.exe
else
    UNAME_S := $(shell uname -s)
    ifeq ($(UNAME_S), Linux)
        TARGET_EXTENSION=
    endif
    ifeq ($(UNAME_S), Darwin)
        TARGET_EXTENSION=
    endif
endif

# Generated by Drasil v0.1-alpha

# Project Name: NameGenExample

build: NameGenExample$(TARGET_EXTENSION)

NameGenExample$(TARGET_EXTENSION): NameGenExample.hpp NameGenExample.cpp
	"$(CXX)" NameGenExample.cpp --std=c++11 -o NameGenExample$(TARGET_EXTENSION)

run: build
	./NameGenExample$(TARGET_EXTENSION) $(RUNARGS)

.PHONY: build run
